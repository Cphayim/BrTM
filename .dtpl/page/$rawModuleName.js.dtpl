/**
 * $rawModuleName 页面逻辑
 * Create by $user at $datetime
 */

import {pagify, MyPage, wxp} from 'base/'

@pagify()
export default class ${ModuleName}Page extends MyPage {
  
  /**
   * 页面名
   */
  pageName = '$rawModuleName'

  /**
   * 页面数据
   */
  data = {

  }
  
  /**
   * 初始化页面
   * 重写父类的 _init()
   * 异步调用栈 onLoad -> _origin -> _init
   */
  async _init(){
    
  }
}
