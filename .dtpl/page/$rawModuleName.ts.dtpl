/**
 * $rawModuleName 页面逻辑
 * Created by $user at $datetime
 */

import { pagify, MyPage } from 'base/'

@pagify()
export default class ${ModuleName}Page extends MyPage {

  readonly pageName = '$rawModuleName'
  readonly when: 'load' | 'ready' = 'load'

  // 页面数据
  data = {

  }

  /**
   * 初始化页面逻辑
   * 重写父类的 init()
   * 异步调用栈 onLoad/onReady -> origin -> init
   */
  async init(){
    // TODO ...
    return true
  }
}
